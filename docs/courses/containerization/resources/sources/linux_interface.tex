\documentclass{article}

\usepackage[T1]{fontenc}

\usepackage{tikz}
\usetikzlibrary{
%    shapes,
%    arrows,
%    shadows,
  calc,
%    positioning,
%    shapes.symbols,
%    quotes,
  intersections,
  decorations.text,
  arrows.meta,
  shapes.arrows
%    fit,
%    backgrounds
}

\usepackage{amsmath}
\usepackage[active,tightpage]{preview}
\PreviewEnvironment{tikzpicture}
\setlength\PreviewBorder{5pt}

\begin{document}
\begin{tikzpicture}[baseline=(current bounding box.north)]
  % Define coordinates
  \def\Radius{8}
  \def\Sectors{16}
  \def\filletradious{0.025*\Radius}
  \def\AppsOuterRadious{\Radius}
  \def\AppsInnerRadious{14*\Radius / \Sectors}
  \def\AppsLabelRadious{14.5*\Radius / \Sectors}
  \def\OSOuterRadious{10*\Radius / \Sectors}
  \def\OSInnerRadious{8*\Radius / \Sectors}
  \def\OSLabelRadious{9*\Radius / \Sectors}
  \def\KernelRadious{4*\Radius / \Sectors}
  \def\UserspaceConnection{12*\Radius / \Sectors}
  \def\OSConnection{6*\Radius / \Sectors}
  
  % Draw semicircle
  \path[draw,name path=apps_outer,fill=blue!20]
    let
      \p{apps_start_outer} = ($(0,0)+(45:\AppsOuterRadious)$),
      \p{apps_end_inner} = ($(0,0)+(225:\AppsInnerRadious)$),
      \p{os_start_outer} = ($(0,0)+(45:\OSOuterRadious)$),
      \p{os_end_inner} = ($(0,0)+(225:\OSInnerRadious)$)
    in
    (\x{apps_start_outer},\y{apps_start_outer}) coordinate (apps_start_outer)
      arc[start angle=45, end angle=225, radius=\AppsOuterRadious] coordinate (apps_end_outer)
      to[out=-45,in=-45] (\x{apps_end_inner}, \y{apps_end_inner}) coordinate (apps_end_inner)
      arc[start angle=225, end angle=45, radius=\AppsInnerRadious] coordinate (apps_start_inner)
      to[out=-45,in=-45] coordinate[pos=0.5] (userspace_link) (apps_start_outer)
      -- cycle
    (\x{os_start_outer}, \y{os_start_outer}) coordinate (os_start_outer)
      arc[start angle=45, end angle=225, radius=\OSOuterRadious] coordinate (os_end_outer)
      to[out=-45,in=-45] (\x{os_end_inner}, \y{os_end_inner}) coordinate (os_end_inner)
      arc[start angle=225, end angle=45, radius=\OSInnerRadious] coordinate (os_start_inner)
      to[out=-45,in=-45] coordinate[pos=0.5] (os_link) (os_start_outer)
      -- cycle
    (0,0) coordinate (kernel) circle[radius=\KernelRadious]
  ;

  \path[draw,fill=yellow!30]
    ($(0,0) + (45:\OSOuterRadious+\filletradious)$) coordinate (user_space_connection_0)
      to[out=-45, in=-45] ($(0,0) + (45:\AppsInnerRadious-\filletradious)$) coordinate (user_space_connection_1)
      arc[start angle=45, end angle=135, radius={\AppsInnerRadious-\filletradious}] ($(0,0) + (135:\AppsInnerRadious-\filletradious)$) coordinate (user_space_connection_2)
      to[out=-135, in=-135]
        coordinate[pos=0.5] (os_interface_control_input)
        ($(0,0) + (135:\OSOuterRadious+\filletradious)$) coordinate (user_space_connection_3)
      arc[start angle=135, end angle=45, radius={\OSOuterRadious+\filletradious}] (user_space_connection_0)
      -- cycle
    ($(0,0) + (45:\KernelRadious+\filletradious)$) coordinate (os_connection_0)
      to[out=-45, in=-45] ($(0,0) + (45:\OSInnerRadious-\filletradious)$) coordinate (os_connection_1)
      arc[start angle=45, end angle=135, radius={\OSInnerRadious-\filletradious}] ($(0,0) + (135:\OSInnerRadious-\filletradious)$) coordinate (os_connection_2)
      to[out=-135, in=-135]
        coordinate[pos=0.5] (kernel_interface_control_input)
        ($(0,0) + (135:\KernelRadious+\filletradious)$) coordinate (os_connection_3)
      arc[start angle=135, end angle=45, radius={\KernelRadious+\filletradious}] (os_connection_0)
      -- cycle
  ;
  \def\OSControlOutAngle{146}
  \path[draw, rounded corners, line width=2pt, >=latex, ->]
    ($(0,0)+(\OSControlOutAngle:\OSOuterRadious)$)
      -- ($(\OSControlOutAngle:0.505*(\OSOuterRadious+\AppsInnerRadious)$) 
      to[out={\OSControlOutAngle-90}, in=-135] (os_interface_control_input)
  ;
  \def\KernelControlOutAngle{156}
  \path[draw, rounded corners, line width=2pt, >=latex, ->]
    ($(0,0)+(\KernelControlOutAngle:\KernelRadious)$)
      -- ($(\KernelControlOutAngle:0.515*(\KernelRadious+\OSInnerRadious)$) 
      to[out={\KernelControlOutAngle-90}, in=-135] (kernel_interface_control_input)
  ;

  \path[postaction={
    decorate,
    decoration={
      text along path,
      text align={center, right indent=0em},
      raise=0pt,
      text format delimiters={[}{]},
      text={[\large\bfseries]Filesystem hierarchy [\large\bfseries\ttfamily](man hier)}
    }
  }]
    let
      \n{rad} = {\OSOuterRadious+0.7*(\AppsInnerRadious-\OSOuterRadious)}
    in
    ($(0,0)+(130:\n{rad})$) arc[start angle=130, end angle=40, radius={\n{rad}}]
  ;
  \path[postaction={
    decorate,
    decoration={
      text along path,
      text align={left},
      raise=0pt,
      text format delimiters={[}{]},
      text={[\ttfamily]PATH}
    }
  }]
    let
      \n{rad} = {\OSOuterRadious+0.5*(\AppsInnerRadious-\OSOuterRadious)-\filletradious}
    in
    ($(0,0)+(130:\n{rad})$) arc[start angle=130, end angle=90, radius={\n{rad}}]
  ;
  \path[postaction={
    decorate,
    decoration={
      text along path,
      text align={left},
      raise=0pt,
      text format delimiters={[}{]},
      text={[\ttfamily]LD{\char95}LIBRARY{\char95}PATH}
    }
  }]
    let
      \n{rad} = {\OSOuterRadious+0.3*(\AppsInnerRadious-\OSOuterRadious)-\filletradious}
    in
    ($(0,0)+(130:\n{rad})$) arc[start angle=130, end angle=90, radius={\n{rad}}]
  ;
  \path[postaction={
    decorate,
    decoration={
      text along path,
      text align={left},
      raise=0pt,
      text format delimiters={[}{]},
      text={[\ttfamily]Rpath/Runpath}
    }
  }]
    let
      \n{rad} = {\OSOuterRadious+0.5*(\AppsInnerRadious-\OSOuterRadious)-\filletradious}
    in
    ($(0,0)+(90:\n{rad})$) arc[start angle=90, end angle=50, radius={\n{rad}}]
  ;
  \path[postaction={
    decorate,
    decoration={
      text along path,
      text align={left},
      raise=0pt,
      text format delimiters={[}{]},
      text={symbolic/hard links}
    }
  }]
    let
      \n{rad} = {\OSOuterRadious+0.3*(\AppsInnerRadious-\OSOuterRadious)-\filletradious}
    in
    ($(0,0)+(90:\n{rad})$) arc[start angle=90, end angle=50, radius={\n{rad}}]
  ;
  \path[postaction={
    decorate,
    decoration={
      text along path,
      text align={center, right indent=0em},
      raise=0pt,
      text format delimiters={[}{]},
      text={Subsystems: [\ttfamily]namespaces,cgroups}
    }
  }]
    let
      \n{rad} = {\KernelRadious+0.7*(\OSInnerRadious-\KernelRadious)-\filletradious}
    in
    ($(0,0)+(140:\n{rad})$) arc[start angle=140, end angle=40, radius={\n{rad}}]
  ;
  \path[postaction={
    decorate,
    decoration={
      text along path,
      text align={center, right indent=0em},
      raise=0pt,
      text format delimiters={[}{]},
      text={Tools: [\ttfamily]chroot,fakeroot}
    }
  }]
    let
      \n{rad} = {\KernelRadious+0.4*(\OSInnerRadious-\KernelRadious)-\filletradious}
    in
    ($(0,0)+(140:\n{rad})$) arc[start angle=140, end angle=40, radius={\n{rad}}]
  ;

  \path[]
    let
      \p{userspace_connection_0} = ($(0,0)+(180:\UserspaceConnection)$)
    in
    (\x{userspace_connection_0}, \y{userspace_connection_0}) coordinate (userspace_connection_0)
         node[
           double arrow,
           draw=black,
           fill=green,
           minimum width = 6pt,
           double arrow head extend=3pt,
           minimum height=12mm,
           rotate=180
         ] (userspace_connection_arrow_0) {}
  ;
  \path[]
    let
      \p{os_connection_0} = ($(0,0)+(180:\OSConnection)$)
    in
    (\x{os_connection_0}, \y{os_connection_0}) coordinate (os_connection_0)
         node[
           double arrow,
           draw=black,
           fill=green,
           minimum width = 6pt,
           double arrow head extend=3pt,
           minimum height=12mm,
           rotate=180
         ] (os_connection_arrow_0) {}
  ;

  \path[postaction={
    decorate,
    decoration={
      text along path,
      text align={center, right indent=0em},
      raise=4pt,
      text={|\large\bfseries|User space}
    }
  }]
    let
      \p{apps_start_label} = ($(0,0)+(135:\AppsLabelRadious)$)
    in
    (\x{apps_start_label},\y{apps_start_label}) arc[start angle=135, end angle=45, radius=\AppsLabelRadious]
  ;
  \path[postaction={
    decorate,
    decoration={
      text along path,
      text align={center, right indent=0em},
      raise=4pt,
      text={(about 2000 subroutines)}
    }
  }]
    let
      \p{apps_start_info} = ($(0,0)+(135:\OSInnerRadious)$)
    in
    (\x{apps_start_info},\y{apps_start_info}) arc[start angle=135, end angle=45, radius=\OSInnerRadious]
  ;
  \path[minimum height=1cm, postaction={
    decorate,
    decoration={
      text along path,
      text align={center, right indent=0em},
      raise=4pt,
      text={|\large\bfseries|Operating system}
    }
  }]
    let
      \p{os_start_label} = ($(0,0)+(135:\OSLabelRadious)$)
    in
    (\x{os_start_label},\y{os_start_label}) arc[start angle=135, end angle=45, radius=\OSLabelRadious]
  ;
  \path[postaction={
    decorate,
    decoration={
      text along path,
      text align={center, right indent=0em},
      raise=4pt,
      text={(about 380 system calls)}
    }
  }]
    let
      \p{os_start_info} = ($(0,0)+(200:\KernelRadious)$)
    in
    (\x{os_start_info},\y{os_start_info}) arc[start angle=200, end angle=340, radius=\KernelRadious]
  ;
  \path[]
    (kernel) node[] (kernel_label) {\large\bfseries Kernel}
  ;

  \node[
    draw,
    fill=green!20,
    rectangle,
    rounded corners,
    minimum height=10cm,
    inner sep=12pt
  ]
    (application) at ($(1.2*\Radius,0)$) {\Large\bfseries Application}
  ;

  \path[draw,line width=4pt]
    ($(application.west)!0.6!(application.north west)$) edge[>=latex,<->] (userspace_link)
    ($(application.west)!0.3!(application.north west)$) edge[>=latex,<->] (os_link)
    (application.west) edge[>=latex,<->] (\KernelRadious,0) node[] (kernel_link) {}
  ;
  
\end{tikzpicture}
\end{document}
